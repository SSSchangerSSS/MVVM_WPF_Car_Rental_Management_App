namespace ViewModels.Enums
{
    /// <summary>
    /// <see cref="ViewModelTypes"/> is an enum that include all types of view models
    /// </summary>
    public enum ViewModelTypes
    {
        MainMenu,
        CustomersView,
        AddCustomer,
        EditCustomer,
        CarsView,
        AddCar,
        EditCar,
        RentalsView,
        AddRental,
        EditRental
    }
}
